# Портфолио сайт с админ панелью

Современный портфолио сайт с красивым дизайном, анимациями и админ панелью для управления проектами, навыками, контактами и настройками.

## Технологии

- **Frontend**: React + Vite
- **Backend**: Node.js + Express
- **База данных**: SQLite (sql.js - pure JavaScript, no compilation needed)
- **Анимации**: GSAP
- **3D Графика**: OGL (WebGL)
- **Роутинг**: React Router
- **Иконки**: React Icons

## Особенности

- ✨ Красивый фон с анимированными частицами (Particles)
- 🎨 Современная навигация с анимациями (CardNav)
- 👤 Интерактивная карточка профиля с 3D эффектами (ProfileCard)
- 📱 Адаптивный дизайн
- 🔐 Админ панель с авторизацией
- 📝 CRUD операции для проектов, навыков, контактов
- 🎭 Плавные анимации и переходы
- 💬 Форма обратной связи с сохранением в админ панель
- 🎯 Управление навыками с прогресс-барами
- ⚙️ Управление настройками сайта через админ панель

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Запустите проект (фронтенд + бэкенд):
```bash
npm run dev
```

Или раздельно:

```bash
# Терминал 1 - Фронтенд
npm run dev:client

# Терминал 2 - Бэкенд
npm run dev:server
```

## Доступ

- **Фронтенд**: http://localhost:3000
- **Бэкенд API**: http://localhost:3001
- **Админ панель**: http://localhost:3000/admin

## Где изменять различные элементы

### 📝 Описание под заголовком (Hero Description)

**Через админ панель (рекомендуется):**
1. Войдите в админ панель: http://localhost:3000/admin
2. Перейдите во вкладку "Настройки"
3. Найдите поле "Описание под заголовком"
4. Измените текст и нажмите "Сохранить описание"

**Через код (дефолтное значение):**
- Файл: `server/database.js`
- Строки: **93-101**
- Измените текст в строке 97:
```javascript
'Создаю современные веб-приложения с красивым дизайном и отличным пользовательским опытом'
```

### 📧 Контакты (Email, Telegram, GitHub, LinkedIn)

**Через админ панель (рекомендуется):**
1. Войдите в админ панель: http://localhost:3000/admin
2. Перейдите во вкладку "Настройки"
3. Найдите раздел "Контакты"
4. Измените нужные поля:
   - Email
   - Telegram
   - GitHub
   - LinkedIn
5. Нажмите "Сохранить контакты"

**Через код (дефолтные значения):**
- Файл: `server/database.js`
- Строки: **103-121**
- Измените значения в массиве `defaultContacts`:
```javascript
const defaultContacts = [
  { key: 'contactEmail', value: 'contact@esteria.com' },
  { key: 'contactTelegram', value: '@esteria' },
  { key: 'contactGitHub', value: 'github.com/esteria' },
  { key: 'contactLinkedIn', value: 'linkedin.com/in/esteria' }
]
```

### 👤 Карточка профиля (ProfileCard)

**Где изменить:**
- Файл: `src/pages/Home.jsx`
- Строки: **123-143**

**Доступные параметры:**
- `name` - Имя (строка 124)
- `title` - Должность (строка 125)
- `handle` - Хендл/никнейм (строка 126)
- `status` - Статус доступности (строка 127)
- `contactText` - Текст кнопки связи (строка 128)
- `avatarUrl` - URL аватара (строка 129, пустая строка = без аватара)
- `behindGlowEnabled` - Эффект свечения (строка 133, false = отключен)

**Пример:**
```jsx
<ProfileCard
  name="esteria"
  title="Full Stack Разработчик"
  handle="esteria"
  status="Доступен для работы"
  contactText="Связаться"
  avatarUrl=""
  showUserInfo={true}
  enableTilt={true}
  enableMobileTilt={false}
  behindGlowEnabled={false}
/>
```

### 🧭 Навигация (CardNav)

**Где изменить:**
- Файл: `src/pages/Home.jsx`
- Строки: **13-40**

**Структура навигации:**
```javascript
const navItems = [
  {
    label: "Обо мне",           // Название карточки
    bgColor: "#0D0716",         // Цвет фона
    textColor: "#fff",          // Цвет текста
    links: [
      { label: "Навыки", href: "#skills", ariaLabel: "Мои навыки" },
      { label: "Опыт", href: "#experience", ariaLabel: "Опыт работы" }
    ]
  },
  // ... другие элементы
]
```

### 🎨 Заголовок страницы (Hero Title)

**Где изменить:**
- Файл: `src/pages/Home.jsx`
- Строки: **115-117**

**Текущий текст:**
```jsx
<h1 className="hero-title">
  Привет, я <span className="gradient-text">Разработчик</span>
</h1>
```

### 🎯 Навыки (Skills)

**Через админ панель (рекомендуется):**
1. Войдите в админ панель: http://localhost:3000/admin
2. Перейдите во вкладку "Навыки"
3. Добавьте новый навык или отредактируйте существующий
4. Укажите:
   - Название
   - Уровень (0-100%)
   - Цвет (hex код)
   - Порядок сортировки

**Через код (дефолтные навыки):**
- Файл: `server/database.js`
- Строки: **124-154** (в функции `initDatabase`)
- Найдите массив `defaultSkills` и измените его

### 📁 Проекты

**Через админ панель (рекомендуется):**
1. Войдите в админ панель: http://localhost:3000/admin
2. Перейдите во вкладку "Проекты"
3. Добавьте новый проект или отредактируйте существующий
4. Заполните:
   - Название
   - Описание
   - Теги (через запятую)
   - Ссылка
   - URL изображения

### 💬 Сообщения от пользователей

**Просмотр и управление:**
1. Войдите в админ панель: http://localhost:3000/admin
2. Перейдите во вкладку "Сообщения"
3. Вы увидите все сообщения с формы обратной связи
4. Можете:
   - Отметить как прочитанное (NEW → SUCCESSFUL с анимацией)
   - Удалить сообщение (с красивой анимацией)

### 🎨 Цвета и стили

**Основные цвета сайта:**
- Файл: `src/pages/Home.css` - стили главной страницы
- Файл: `src/pages/Admin.css` - стили админ панели
- Файл: `src/components/*/ComponentName.css` - стили компонентов

**Градиенты:**
- Ищите `linear-gradient` в CSS файлах
- Можно изменить цвета градиентов для персонализации

### ⚙️ Настройки частиц (Particles Background)

**Где изменить:**
- Файл: `src/pages/Home.jsx`
- Строки: **87-97**

**Параметры:**
```jsx
<Particles
  particleColors={['#ffffff', '#ffffff']}  // Цвета частиц
  particleCount={200}                      // Количество частиц
  particleSpread={10}                      // Разброс частиц
  speed={0.1}                              // Скорость анимации
  particleBaseSize={100}                   // Размер частиц
  moveParticlesOnHover={true}              // Движение при наведении
  alphaParticles={false}                   // Прозрачность
  disableRotation={false}                  // Вращение
/>
```

## Структура проекта

```
├── src/
│   ├── components/
│   │   ├── Particles/          # Компонент с анимированными частицами
│   │   ├── CardNav/            # Навигация с анимациями
│   │   ├── ProfileCard/        # Интерактивная карточка профиля
│   │   ├── ProjectCard/        # Карточка проекта
│   │   ├── SkillsSection/      # Секция навыков
│   │   ├── ExperienceSection/  # Секция опыта работы
│   │   └── ContactSection/     # Секция контактов
│   ├── pages/
│   │   ├── Home.jsx            # Главная страница портфолио
│   │   └── Admin.jsx           # Админ панель
│   ├── App.jsx
│   └── main.jsx
├── server/
│   ├── index.js                # Express сервер + API
│   ├── database.js             # SQLite база данных (операции с БД)
│   └── portfolio.db            # SQLite файл БД (создается автоматически)
├── package.json
└── vite.config.js              # Конфигурация Vite (прокси для API)
```

## API Endpoints

### Публичные
- `GET /api/projects` - Получить все проекты
- `GET /api/skills` - Получить все навыки
- `GET /api/settings/:key` - Получить настройку (например, heroDescription)
- `GET /api/contacts-info` - Получить контактную информацию
- `POST /api/contacts` - Отправить сообщение через форму обратной связи

### Требуется авторизация
- `POST /api/auth/login` - Вход в админ панель
- `GET /api/auth/verify` - Проверка токена
- `POST /api/projects` - Создать проект
- `PUT /api/projects/:id` - Обновить проект
- `DELETE /api/projects/:id` - Удалить проект
- `GET /api/admin/skills` - Получить все навыки (админ)
- `POST /api/skills` - Создать навык
- `PUT /api/skills/:id` - Обновить навык
- `DELETE /api/skills/:id` - Удалить навык
- `GET /api/contacts` - Получить все сообщения
- `GET /api/contacts/unread-count` - Получить количество непрочитанных
- `PUT /api/contacts/:id/read` - Отметить как прочитанное
- `DELETE /api/contacts/:id` - Удалить сообщение
- `PUT /api/settings/:key` - Обновить настройку
- `PUT /api/contacts-info` - Обновить контакты

## Компоненты

### Particles
Компонент для создания анимированного фона с частицами используя WebGL (OGL).

### CardNav
Навигационный компонент с плавными анимациями и выпадающими карточками. Использует GSAP для анимаций.

### ProfileCard
Интерактивная карточка профиля с 3D эффектами наклона, голографическими эффектами и анимацией при наведении.

### ProjectCard
Карточка проекта с изображением, описанием, тегами и ссылкой.

### SkillsSection
Секция навыков с анимированными прогресс-барами. Загружает навыки из базы данных.

### ExperienceSection
Секция опыта работы с временной шкалой.

### ContactSection
Секция контактов с формой обратной связи и ссылками на социальные сети.

## База данных

Все данные сохраняются в SQLite базу данных `server/portfolio.db`. Файл создается автоматически при первом запуске сервера.

### Таблицы

1. **projects** - Проекты портфолио
   - id, title, description, tags, link, image, createdAt, updatedAt

2. **admins** - Администраторы
   - id, username, password (хэшированный)

3. **contacts** - Сообщения от пользователей
   - id, name, email, contactMethod, message, createdAt, read

4. **skills** - Навыки
   - id, name, level, color, sortOrder, createdAt

5. **settings** - Настройки сайта
   - key, value, updatedAt
   - Используемые ключи:
     - `heroDescription` - Описание под заголовком
     - `contactEmail` - Email для контактов
     - `contactTelegram` - Telegram
     - `contactGitHub` - GitHub
     - `contactLinkedIn` - LinkedIn

### Инициализация

При первом запуске создаются:
- Все таблицы
- Админ пользователь `root/root` (если его нет)
- Дефолтное описание под заголовком
- Дефолтные контакты
- Дефолтные навыки (если таблица пустая)

## Разработка

Для сборки проекта:
```bash
npm run build
```

Для предпросмотра продакшн сборки:
```bash
npm run preview
```

## Автоматическое обновление

Сайт автоматически обновляет данные каждые 2 секунды без перезагрузки страницы:
- Описание под заголовком
- Контакты
- Навыки
- Проекты

Это означает, что изменения в админ панели применяются на главной странице в течение 2 секунд.

## Анимации

### Статусы сообщений
- При отметке как прочитанное: плавная анимация перехода NEW → SUCCESSFUL
- При удалении сообщения: плавная анимация исчезновения с движением вправо

### Бейджи статусов
- "NEW" - фиолетовый градиент с пульсацией
- "SUCCESSFUL" - зеленый градиент с эффектом свечения

## Важно

⚠️ В продакшене обязательно измените `JWT_SECRET` в `server/index.js` на безопасный случайный ключ!

⚠️ Используется sql.js - чистая JavaScript библиотека, не требует компиляции C++ кода или build tools. Работает из коробки!

⚠️ База данных `portfolio.db` добавляется в `.gitignore` - не коммитьте её в репозиторий!

## Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте, что сервер запущен на порту 3001
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Проверьте консоль сервера на наличие ошибок
4. Убедитесь, что база данных создана и доступна

## Лицензия

MIT License - используйте свободно для своих проектов!
